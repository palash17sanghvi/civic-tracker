# crontab for civic-tracker data refresh
# Runs the ingestion pipeline every 6 hours (at 00:00, 06:00, 12:00, 18:00)
# Must end with a blank line for cron to parse correctly.

0 */6 * * * /app/scripts/refresh_data.sh

